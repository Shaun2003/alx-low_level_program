#include <stdio.h>

/**
 * main - prints alpha in lower and upper case
 * Return: Always 0
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'
		putchar(ch);

	for (ch = 'A'; ch <= 'Z'
		putchar(ch);

	putchar('\n');

	return (0);
}
